// Component removed — user requested to revert FormInsights addition.
// Keep a small noop default export so imports (if any slipped through) don't break builds.
"use client";
import React from 'react';

export default function FormInsights() {
  return null;
}
